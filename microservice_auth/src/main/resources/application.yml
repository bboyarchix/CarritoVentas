spring:
  application:
    name: microservice-authService
  profiles:
    active: development
  config:
    import: optional:configserver:http://root:123456@localhost:7070  # Actualiza la contrase�a aqu�
